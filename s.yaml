edition: 1.0.0
access: sub

services:
  blog:
    component: devsapp/website
    actions:
      pre-deploy:
        - run: npm run build
          path: ./
    props:
      bucket: shihuali
      src:
        publishDir: ./docs-dist
        index: index.html
      region: cn-hangzhou
      hosts:
        - host: auto
